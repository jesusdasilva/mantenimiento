{# GERENCIA LISTADO#}
{% extends "layout_app.html.twig" %}
{% block contenido_app %}
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Buscar Gerencia</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
{% include 'mensaje_alerta.html.twig' %}{#MENSAJE#}
    <div class="row">
      <div class="col-lg-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            Listado de las Gerencia
          </div>
          <!-- /.panel-heading -->
          <div class="panel-body">
            <div class="dataTable_wrapper">
              <table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <thead>
                  <tr>
                    <th>Item</th>
                    <th>Nombre de la Gerencia</th>
                    <th>Acción</th>
                  </tr>
                </thead>
                <tfoot>
                  <tr>
                    <th>Item</th>
                    <th>Nombre de la Gerencia</th>
                    <th>Acción</th>
                  </tr>
                </tfoot>
                <tbody>
{% for registro in gerencias %}
                  <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ registro.gerencia_nombre }}</td>
                    <td>
                      <a href="{{ app.url_generator.generate('gerenciaBuscar', {'gerenciaId': registro.gerencia_id}) }}">
                        <button type="button" class="btn btn-default btn-xs">
                        <span class="glyphicon glyphicon-edit"></span>Modificar</button>
                      </a>
                      <a href="{{ app.url_generator.generate('gerenciaEliminar', {'gerenciaId': registro.gerencia_id}) }}" onclick="return confirm_delete();">
                        <button type="button" class="btn btn-default btn-xs">
                        <span class="glyphicon glyphicon-trash"></span>Eliminar</button>
                      </a>
                    </td>
                  </tr>
{% endfor %}
              </tbody>
            </table>
          </div>
          <!-- /.table-responsive -->
        </div>
        <!-- /.panel-body -->
      </div>
      <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
</div>
<!-- /#page-wrapper -->
{% endblock %}
