{# GERENCIA  DATOS #}
{% extends  "layout_app.html.twig" %}
{% block contenido_app %}
<div id="page-wrapper">
  <div class="row">
    <div class="col-lg-12">
{% if editar == TRUE  %}
      <h1 class="page-header">Modificar Gerencia</h1>
{% else %}
      <h1 class="page-header">Nueva Gerencia</h1>
{% endif %}
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
  <div class="row">
    <div class="col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          Datos de la Gerencia
        </div>
        <div class="panel-body">
          <div class="row">
            <div class="col-lg-6">
{% if editar == TRUE  %}
              <form role="form" action="{{ app.url_generator.generate('gerenciaGuardarActualizar') }}" method="post">
                <input type="hidden" name="gerencia-id" id="gerencia-id" value="{{ gerencia_id }}" />
                <input type="hidden" name="gerencia-nombre-h" id="gerencia-nombre-h" value="{{ gerencia_nombre }}" />
{% else %}
              <form role="form" action="{{ app.url_generator.generate('gerenciaGuardarNuevo') }}" method="post">
{% endif %}
{#MENSAJE ALERT#}
{% include 'mensaje_alerta.html.twig' %}
{# NOMBRE #}
                <div class="form-group">
                  <label>Nombre</label>
                  <input required id="gerencia-nombre" name="gerencia-nombre" class="form-control" type="text" {% if editar == TRUE %} disabled {% endif %} value="{{ gerencia_nombre|default('') }}"   maxlength="20">
                  <p class="help-block">Introduzca el nombre de la Gerencia.</p>
                </div>
{# /NOMBRE #}
{# OBSERVACIONES #}
                <div class="form-group">
                  <label>Observaciones</label>
                  <textarea id="gerencia-observacion" name="gerencia-observacion" class="form-control" rows="3">{{ gerencia_observacion|default('')  }}</textarea>
                </div>
{# /OBSERVACIONES #}
                <button type="submit" class="btn btn-primary">Guardar</button>
              </form>
            </div>
            <!-- /.col-lg-6 -->
          </div>
          <!-- /.row (nested) -->
        </div>
        <!-- /.panel-body -->
      </div>
      <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
</div>
<!-- /#page-wrapper -->
{% endblock %}
